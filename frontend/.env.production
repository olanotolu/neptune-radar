# Production build settings — vite loads this instead of .env when building
# with mode=production. The API is same-origin in prod (one container serves
# both the dashboard and the API), so the base URL must be EMPTY here.
# (.env holds VITE_API_URL=http://localhost:8080 for local dev only.)
VITE_API_URL=
